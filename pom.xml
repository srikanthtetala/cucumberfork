<build>
  <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <version>3.8.1</version>
      <configuration>
        <source>1.8</source> <!-- or 11 if you're using Java 11 -->
        <target>1.8</target> <!-- or 11 if you're using Java 11 -->
        <encoding>UTF-8</encoding> <!-- Set the file encoding explicitly -->
      </configuration>
    </plugin>
  </plugins>
</build>
